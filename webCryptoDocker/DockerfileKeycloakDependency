FROM ubuntu:18.04

#RUN apt-get update && apt-get install -y wget
#RUN wget https://downloads.jboss.org/keycloak/6.0.1/adapters/keycloak-oidc/keycloak-wildfly-adapter-dist-6.0.1.tar.gz && \
 #   mkdir /keycloak && \
 #   mv keycloak-wildfly-adapter-dist-6.0.1.tar.gz /keycloak/keycloak-wildfly-adapter-dist.tar.gz

RUN apt-get update && apt-get -y install openssl && rm -rf /var/lib/apt/lists/*

WORKDIR /usr/local/certif/

RUN echo "\n\n\ntest\n\n\n\n" > creation.txt && openssl req -x509 -nodes -days 1 -newkey rsa:2048 -keyout ./tls.key -out ./tls.crt < creation.txt
